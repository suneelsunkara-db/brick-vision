"""Vendored minimal YAML + templating substitutes.

These exist so the Phase -1 round-trip gate can run without external Python
dependencies. Production deployments use the real PyYAML + Jinja2 declared
in `src/brickvision_runtime/pyproject.toml`.
"""
